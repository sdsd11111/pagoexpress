import { Inngest } from "inngest";

// Creación del cliente de Inngest
// El id "pagoexpress" es el nombre único de tu aplicación dentro de Inngest.
export const inngest = new Inngest({ id: "pagoexpress" });
